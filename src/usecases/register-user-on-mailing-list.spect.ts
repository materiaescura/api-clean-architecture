import type { UserData } from '../entities/user-data'

describe('Register user on mailing list use case', () => {
  it('should add user user with complete data to mailing list', async () => {
    const users: UserData[] = []
    console.log(users)
    // const repo: UserRepository = new InMemoryUserRepository(repo)
    // const usecase: RegisterUserOnMailingList = new RegisterUserOnMailingList(repo)
    // const name = 'any_name'
    // const email = 'any@email.com'
    // const response = await usecase.registerUserOnMailingList({email, data})
    // const user = repo.findUserByEmail(email)
    // expect((await user).name).toBe('any_name')
  })
})
